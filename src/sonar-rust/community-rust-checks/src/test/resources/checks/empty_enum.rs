
enum WebEvent {
    // An `enum` may either be `unit-like`,
    PageLoad,
    PageUnload,
    // like tuple structs,
    KeyPress(char),
    Paste(String),
    // or c-like structures.
    Click { x: i64, y: i64 },
}

/* Noncompliant@+1 {{Either remove or fill this empty enumeration.}}*/
enum My_Empty_num {}