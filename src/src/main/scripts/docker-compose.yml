version: "3.3"
services:
  sonar:
    image: sonarqube:${sonar_version_server}
    ports:
      - "9000:9000"
    networks:
      - sonarnet
    depends_on:
      - db
    environment:
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
      SONAR_JDBC_URL: jdbc:postgresql://db:5432/sonarqube
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: 'true'
    volumes:
      - type: bind
        source: ${basedir}/java-plugin/target/ecocode-java-plugin-${project.version}.jar
        target: /opt/sonarqube/extensions/plugins/ecocode-java-plugin-${project.version}.jar
      - type: bind
        source: ${basedir}/php-plugin/target/ecocode-php-plugin-${project.version}.jar
        target: /opt/sonarqube/extensions/plugins/ecocode-php-plugin-${project.version}.jar
      - type: bind
        source: ${basedir}/python-plugin/target/ecocode-python-plugin-${project.version}.jar
        target: /opt/sonarqube/extensions/plugins/ecocode-python-plugin-${project.version}.jar
      - type: bind
        source: ${basedir}/android-plugin/target/ecocode-rules-${project.version}.jar
        target: /opt/sonarqube/extensions/plugins/ecocode-rules-${project.version}.jar
      # - "conf:/opt/sonarqube/conf"
      - "extensions:/opt/sonarqube/extensions"
      - "logs:/opt/sonarqube/logs"
      - "data:/opt/sonarqube/data"

  db:
    image: postgres:12
    container_name: postgresql
    networks:
      - sonarnet
    volumes:
      - pg_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonarqube
      PGDATA: pg_data:/var/lib/postgresql/data/pgdata

networks:
  sonarnet:
    driver: bridge

volumes:
  data:
  logs:
  extensions:
  pg_data:
