<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent" android:layout_height="match_parent" android:background="#FFFFFF"><!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
    <RelativeLayout android:layout_width="match_parent" android:layout_height="match_parent">
        <RelativeLayout android:layout_width="match_parent" android:layout_height="match_parent">
            <ImageView android:layout_width="match_parent" android:layout_height="match_parent" android:src="#000000"/>
            <ImageView android:src="#0FFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <ImageView android:background="#000000"/>
            <ImageView android:background="#0FFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <ImageView android:foreground="#000000"/>
            <ImageView android:foreground="#0FFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:foreground="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:foreground="#000000"/>
            <TextView android:foregroundTint="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:foregroundTint="#000000"/>
            <TextView android:textColor="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:textColor="#000000"/>
            <TextView android:tint="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:tint="#000000"/>
            <TextView android:textColorHighlight="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:textColorHighlight="#000000"/>
            <TextView android:textColorHint="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:textColorHint="#000000"/>
            <TextView android:textColorLink="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:textColorLink="#000000"/>
            <TextView android:shadowColor="#FFFFFF"/> <!-- Noncompliant {{Avoid using too bright colors for (AM)OLED screens.}} -->
            <TextView android:shadowColor="#000000"/>
        </RelativeLayout>
    </RelativeLayout>
</RelativeLayout>
