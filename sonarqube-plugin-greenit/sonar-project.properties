# must be unique in a given SonarQube instance
sonar.projectKey=sonar-custom-plugin-example

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

sonar.eslint.reportPaths=/opt/project/eslint-report.json
sonar.css.stylelint.reportPaths=/opt/project/stylelint-report.json
sonar.java.binaries=/opt/project/java-plugin/target/classes
sonar.scm.disabled=true